export function SlashMenuHint() {
  return (
    <div className="mt-4 flex items-center gap-2 text-xs text-muted">
      <span className="rounded-full border border-white/8 px-2 py-0.5 font-mono text-[11px] text-soft-linen">/</span>
      <span>Type slash for blocks, paste screenshots from the clipboard, or drop images directly into the document.</span>
    </div>
  );
}
